# Kalimba Tabs

Rewrite of KalimbaLibre using Electron.

## Setup

After cloning the repo, in the root folder run:

`npm install`

then

`npm run electron-dev`

## Build

To make a build of the app, run `npm run electron-pack` in the root folder
